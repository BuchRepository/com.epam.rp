name: CI - .NET Tests with Chrome

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    container:
      image: selenium/standalone-chrome:latest
      options: --user root --shm-size=2g

    env:
      DOTNET_ROOT: /usr/share/dotnet

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        working-directory: ./ReportPortal
        run: dotnet restore

      - name: Build solution
        working-directory: ./ReportPortal
        run: dotnet build --configuration Release --no-restore

      - name: Create appsettings.json from secrets
        working-directory: ./ReportPortal
        run: |
          echo "{
            \"LOGIN\": \"${{ secrets.TEST_LOGIN }}\",
            \"PASSWORD\": \"${{ secrets.TEST_PASSWORD }}\",
            \"Serilog\": {
              \"Using\": [\"Serilog.Sinks.Console\", \"Serilog.Sinks.File\"],
              \"MinimumLevel\": {
                \"Default\": \"Information\",
                \"Override\": {
                  \"Microsoft\": \"Warning\",
                  \"System\": \"Warning\"
                }
              },
              \"WriteTo\": [
                {
                  \"Name\": \"Console\"
                },
                {
                  \"Name\": \"File\",
                  \"Args\": {
                    \"path\": \"logs/logfile.log\",
                    \"restrictedToMinimumLevel\": \"Information\"
                  }
                }
              ]
            }
          }" > appsettings.json

      - name: Print working directory
        run: pwd

      - name: Check logs folder
        working-directory: ./ReportPortal
        run: |
          echo "Current working directory:" &&
          pwd &&
          echo "Checking contents of ./bin/Release/net8.0/logs:" &&
          if [ -d ./bin/Release/net8.0/logs ]; then
          ls -l ./bin/Release/net8.0/logs &&
          if [ -f ./bin/Release/net8.0/logs/logfile.log ]; then
          echo "Log file exists:";
          cat ./bin/Release/net8.0/logs/logfile.log;
          else
          echo "LOG FILE DOES NOT EXIST!";
          fi
          else
          echo "Logs folder does not exist!";
          fi

      - name: Run tests
        working-directory: ./ReportPortal
        run: dotnet test --configuration Release --logger "trx;LogFileName=test_results.trx"

      - name: Upload Logs
        uses: actions/upload-artifact@v4
        with:
          name: Test Logs
          path: ./ReportPortal/bin/Release/net8.0/logs/logfile.log

      - name: Find log files
        working-directory: ./ReportPortal
        run: |
          echo "Searching for log files in ./bin/Release/net8.0/logs:" &&
          find ./bin/Release/net8.0/logs -name "*.log"
      
